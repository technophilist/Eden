name: Build
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Change permissions of ./gradlew
        run: chmod +x ./gradlew
      - name: Build the app
        run: ./gradlew build