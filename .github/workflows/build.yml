name: Build
on:
  push:
    branches: [ main,run-tests-on-firebase-emulator ]
jobs:
  #  build:
  #    runs-on: ubuntu-latest
  #    steps:
  #      - name: Checkout code
  #        uses: actions/checkout@v2
  #      - name: Decrypt google services config file
  #        env:
  #          KEY: ${{ secrets.GOOGLE_SERVICES_JSON_DECRYPTION_KEY }}
  #        run: |
  #          cd app/
  #          openssl enc -aes-256-cbc -d -in 752bc914153e2aede6d620a311736e39.txt -out google-services.json -k $KEY
  #      - name: Change permissions of ./gradlew
  #        run: chmod +x ./gradlew
  #      - name: Build the app
  #        run: |
  #          ./gradlew build
  #           rm app/google-services.json
  run-firebase-auth-tests:
    #    needs: build
    runs-on: macos-latest
    env:
      FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
    strategy:
      matrix:
        api-level: [ 23 ]
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Setup node.js
        uses: actions/setup-node@v1
        with:
          node-version: "12"
      - name: Install and start Firebase emulator suit
        run: |
          npm install -g firebase-tools
          firebase emulators:start --project=eden
      - name: Startup emulator and run instrumented tests
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level": ${{ matrix.api-level }}
          script: ./gradlew connectedAndroidTest

